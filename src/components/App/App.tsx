import React from 'react';

import Splash from '../Splash/Splash';

const App: React.FC = () => {
    return (
        <div className="App">
            <Splash />
        </div>
    );
};

export default App;
